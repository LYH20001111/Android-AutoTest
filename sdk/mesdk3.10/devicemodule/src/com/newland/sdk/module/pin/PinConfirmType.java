package com.newland.sdk.module.pin;

public enum PinConfirmType {
	/**
	 * Enter disabled <p>
	 * it will return pinblock automatically after input is completed
	 */
	DISABLE_ENTER,
	/**
	 * Enter enabled<p>
	 * the pinblock will ben returned after press "Confirm" key
	 */
	ENABLE_ENTER
}
